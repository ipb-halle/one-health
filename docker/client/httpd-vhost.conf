LoadModule proxy_module modules/mod_proxy.so
LoadModule proxy_http_module modules/mod_proxy_http.so
LoadModule rewrite_module modules/mod_rewrite.so
LoadModule headers_module modules/mod_headers.so

ProxyPreserveHost On

ProxyPass        /api/ http://server:8080/api/
ProxyPassReverse /api/ http://server:8080/api/

RewriteEngine On

# Do not rewrite API requests.
RewriteCond %{REQUEST_URI} !^/api/

# Do not rewrite Let's Encrypt ACME challenges.
RewriteCond %{REQUEST_URI} !^/\.well-known/

# Serve existing files/directories normally.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# React BrowserRouter fallback.
RewriteRule ^ /index.html [L]